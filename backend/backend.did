service : {
  getAIAssistance: (text) -> (text);
  getStripePublishableKey: () -> (text) query;
  loadWebsite: () -> (text);
  processStripePayment: (text) -> (record {success: bool;});
  publishWebsite: () -> (text);
  saveWebsite: (text) -> ();
  setupStripePayment: (text) -> (record {success: bool;});
}
